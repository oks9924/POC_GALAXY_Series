#include "net_driver.h"

// 내부 함수 선언 (Stub)
static bool init_hardware_interface(void) { return true; } // 임시 return
static void enable_dma(void) {}
static void configure_interrupts(void) {}

bool init_network_interface(void) {
    // 골격만 존재, 아직 로직 없음
    return false; 
}